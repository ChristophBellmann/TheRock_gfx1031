# Runtime-only artifacts (do not commit)
/workspace/
/.venv/
/.python-version
/.ruff_cache/
/.mypy_cache/

# Legacy cache dir (kept for backwards compatibility, not committed)
/_cache/

# Avoid package-path drift: keep only `src/validation_suite/`
/therock_validation/
/rocm_validation/
__pycache__/
*.pyc

# Run only the Ollama workload (plus minimal ROCm sanity).
run:
  profile: ollama
  ask_before_downloads: true
  downloads_enabled: true

steps:
  rocm_sanity: true
  rocm_bench_smoke: false
  miopen_smoke: false
  llama_cpp_docker: false
  ollama: true
  open_interpreter: false
  whisper: false
  mfem_hip: false

